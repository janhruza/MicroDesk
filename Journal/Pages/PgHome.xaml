<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Journal.Pages.PgHome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Journal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    KeyDown="Page_KeyDown">

    <Page.ContextFlyout>
        <MenuFlyout>
            <MenuFlyoutItem Text="Refresh"
                            KeyboardAcceleratorTextOverride="F5"
                            Click="mfUpdate_Click"/>
        </MenuFlyout>
    </Page.ContextFlyout>
    
    <Grid ColumnSpacing="24"
          Padding="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Recent Entries"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   Margin="0,0,0,24"/>

        <ListView Grid.Column="0"
                  Grid.Row="1"
                  x:Name="lvHistory"
                  SelectionChanged="lvHistory_SelectionChanged">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0,0,0,4"/>
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Border Grid.Column="1"
                Grid.Row="1"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="24">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="12">
                    <TextBox x:Name="txtPreviewTitle"
                             FontWeight="Bold"
                             BorderThickness="0"
                             Background="Transparent"
                             IsReadOnly="True"
                             TextWrapping="Wrap"/>
                    
                    <MenuFlyoutSeparator />
                    
                    <TextBox x:Name="txtPreviewContent"
                             BorderThickness="0"
                             Background="Transparent"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

    </Grid>
</Page>
