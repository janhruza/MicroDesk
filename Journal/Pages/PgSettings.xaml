<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Journal.Pages.PgSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Journal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Settings"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   Margin="0,0,0,24"/>

        <ScrollViewer Grid.Row="1" MaxWidth="800" HorizontalAlignment="Left">
            <StackPanel Spacing="12">
                <TextBlock Text="Personalization" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0,12,0,4"/>

                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="App Theme" Style="{ThemeResource BodyTextBlockStyle}"/>
                            <TextBlock Text="Choose which theme to display" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" x:Name="cbxTheme" Width="150" SelectionChanged="cbxTheme_SelectionChanged">
                            <ComboBoxItem Tag="themeLight" Content="Light"/>
                            <ComboBoxItem Tag="themeDark" Content="Dark"/>
                            <ComboBoxItem Tag="themeSystem" Content="System"/>
                        </ComboBox>
                    </Grid>
                </Border>

                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="Window Backdrop" Style="{ThemeResource BodyTextBlockStyle}"/>
                            <TextBlock Text="Select background effect for the application" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" x:Name="cbxBackdrop" Width="150" SelectionChanged="cbxBackdrop_SelectionChanged">
                            <ComboBoxItem Tag="bpNone" Content="None"/>
                            <ComboBoxItem Tag="bpAcrylic" Content="Acrylic"/>
                            <ComboBoxItem Tag="bpMica" Content="Mica"/>
                            <ComboBoxItem Tag="bpMicaAlt" Content="Mica (Alt)"/>
                        </ComboBox>
                    </Grid>
                </Border>

                <TextBlock Text="About" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0,24,0,4"/>
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Journal" Style="{ThemeResource BodyTextBlockStyle}"/>
                        <TextBlock Text="Version 1.0.0" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
