<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Journal.Pages.PgNewEntry"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Journal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <RelativePanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="New Entry"
                       Style="{ThemeResource TitleTextBlockStyle}"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignVerticalCenterWithPanel="True"/>

            <Button x:Name="btnSave"
                    Style="{ThemeResource AccentButtonStyle}"
                    Content="Save Entry"
                    Padding="16,8"
                    Click="btnSave_Click"
                    RelativePanel.AlignRightWithPanel="True"/>
        </RelativePanel>

        <ScrollViewer Grid.Row="1"
                      HorizontalAlignment="Stretch">
            
            <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="32">
                <StackPanel Spacing="24">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Title" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                        <TextBox x:Name="txtTitle"
                                 PlaceholderText="Enter a title..."
                                 Padding="10,6"/>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Content" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                        <TextBox x:Name="txtContent"
                                 AcceptsReturn="True"
                                 MinHeight="300"
                                 TextWrapping="Wrap"
                                 PlaceholderText="Write your thoughts here..."
                                 Padding="10,10"
                                 VerticalContentAlignment="Top"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Page>
