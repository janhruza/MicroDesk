<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Financier.Pages.NewProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Financier.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Tag="New Profile">

    <Grid RowSpacing="16">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <InfoBar Grid.Column="0"
                 Name="ib"
                 IsOpen="True"
                 Severity="Informational"
                 Title="Create profile"
                 Message="In order to continue, please set up a local user profile."/>

        <!--Command bar-->
        <CommandBar Grid.Row="1"
                    OverflowButtonVisibility="Collapsed">

            <AppBarButton Label="Profile Selector"
                          Icon="Switch"
                          Name="btnSwitchProfiles"
                          Click="btnSwitchProfiles_Click"/>

            <AppBarSeparator />

            <!--Clear fields-->
            <AppBarButton Label="Clear"
                          Icon="Clear"
                          Name="btnClear"
                          Click="btnClear_Click"/>

            <!--Create profile-->
            <AppBarButton Label="Create"
                          Icon="Save"
                          Name="btnSave"
                          Click="btnSave_Click"/>

        </CommandBar>

        <StackPanel Grid.Row="2">

            <TextBlock Text="Name"
                       Margin="0,0,0,8"/>

            <!--Profile name-->
            <TextBox Name="txtName"/>

        </StackPanel>

    </Grid>
</Page>
