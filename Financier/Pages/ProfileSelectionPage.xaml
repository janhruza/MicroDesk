<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Financier.Pages.ProfileSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Financier.Pages"
    xmlns:core="using:Financier.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Tag="Select a profile">

    <Grid Padding="24" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Manage Profiles" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold"/>

        <InfoBar Name="ib"
                 Grid.Row="1"
                 IsOpen="False"
                 Margin="0,0,0,8"/>

        <Grid Grid.Row="2" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Saved Profiles" Style="{ThemeResource BodyStrongTextBlockStyle}" Opacity="0.7"/>

            <ListView Grid.Row="1"
                      Name="lvProfiles"
                      Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}"
                      CornerRadius="8"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1"
                      SelectionChanged="lvProfiles_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="core:UserProfile">
                        <Grid Padding="16" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <FontIcon Glyph="&#xE77B;" 
                                      FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                      FontSize="24" 
                                      Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16"/>
                                <TextBlock Text="Local Profile" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button x:Name="btnAccept" 
                    Content="Log In" 
                    Style="{ThemeResource AccentButtonStyle}"
                    IsEnabled="False"
                    Padding="24,8"
                    Click="btnAccept_Click"/>
            
            <Button x:Name="btnNewProfile" 
                    Content="New Profile" 
                    Padding="24,8"
                    Click="btnNewProfile_Click"/>
            
            <Button x:Name="btnRefresh" 
                    Content="Refresh" 
                    Padding="24,8"
                    Click="btnRefresh_Click"/>
        </StackPanel>
    </Grid>
</Page>
